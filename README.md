# XML Template Field Replacer

Набор утилит для автоматической замены префиксов полей в XML-шаблонах Word, используемых в системах документооборота.

## 🚀 Быстрый старт: Веб-интерфейс

Для удобной работы доступен веб-интерфейс, который позволяет генерировать конфигурацию без использования командной строки.

### Установка и запуск

```bash
# Клонирование репозитория
git clone https://github.com/AlexanderKuzikov/Replacer-2.git
cd Replacer-2

# Установка зависимостей
npm install

# Запуск сервера
npm start
```

Откройте браузер: `http://localhost:3000`

### Использование веб-интерфейса

1. **Загрузите файл** - выберите .fdt или .docx файл шаблона
2. **Просмотрите поля** - система автоматически извлечет и покажет все поля из document.xml
3. **Выберите префиксы** - укажите старый и новый префиксы из доступных полей
4. **Нажмите "Сгенерировать"** - config.json будет создан в корне проекта
5. **Запустите обработку** - используйте config.json с CLI утилитами

### Текущий функционал (демонстрация)

✅ Загрузка .fdt и .docx файлов  
✅ Автоматическое извлечение document.xml из архива  
✅ Парсинг всех полей в фигурных скобках `{...}`  
✅ Отображение списка доступных полей  
✅ Выбор префиксов из реальных данных шаблона  
✅ Генерация корректного config.json  
✅ Валидация входных данных  
✅ Современный дизайн интерфейса

### Планируемые улучшения

🔄 Улучшенная работа с полями (группировка, фильтрация)  
🔄 Интеграция CLI утилит в веб-интерфейс  
🔄 Визуализация процесса обработки  
🔄 Обратная сборка обработанного document.xml в .docx/.fdt  
🔄 Предпросмотр изменений (diff старых/новых полей)  
🔄 История операций и batch-обработка

---

## Возможности

```
project/
├── public/                  # Веб-интерфейс
│   ├── index.html           # HTML-страница интерфейса
│   ├── style.css            # Стили интерфейса
│   └── script.js            # Frontend логика
├── server.js                # Express сервер для UI
├── package.json             # Зависимости (express, multer, adm-zip)
├── config.json              # Общий конфигурационный файл
├── analyzer.js              # Утилита 1: Анализ XML и извлечение полей
├── generator.js             # Утилита 2: Генерация карты замен
├── replacer.js              # Утилита 3: Применение замен к XML
├── IN/                      # Входные файлы
│   └── template.xml         # Исходный XML-шаблон
└── OUT/                     # Выходные файлы
    ├── unique_names.json    # Уникальные имена полей
    ├── replacement_map.json # Карта замен
    └── output.xml           # Обработанный XML
```

---

## 💻 Использование CLI (утилиты командной строки)

Для продвинутых пользователей доступны три отдельные утилиты, которые можно запускать независимо.

### 1. Настройка конфигурации

Редактируйте `config.json` (или создайте через веб-интерфейс):

```json
{
  "analyzer": {
    "inputFile": "IN/document.xml",
    "outputFile": "OUT/unique_names.json",
    "originalPrefix": "автомобильПострадавшего.",
    "cleanLogicalConstructions": true,
    "sortByLength": true
  },
  "generator": {
    "inputFile": "OUT/unique_names.json",
    "outputFile": "OUT/replacement_map.json",
    "newPrefix": "арендованноеТс.",
    "encoding": "utf8"
  },
  "replacer": {
    "inputFile": "IN/document.xml",
    "outputFile": "OUT/document.xml",
    "replacementMapFile": "OUT/replacement_map.json"
  }
}
```

### 2. Запуск утилит

```bash
# Шаг 1: Анализ XML и извлечение полей
node analyzer.js
# Найдено 156 фрагментов, извлечено 90 уникальных полей

# Шаг 2: Генерация карты замен
node generator.js
# Создано 90 пар замен (текст + Base64)

# Шаг 3: Применение замен
node replacer.js
# Выполнено 2700+ замен за ~2с
```

---

## Архитектура решения

### Веб-интерфейс (демонстрация)

**server.js** - Express приложение с endpoints:  
- `/` - статический UI  
- `/api/upload-and-analyze` - загрузка файла и извлечение полей  
- `/api/generate-config` - генерация config.json

**Workflow:**
1. Пользователь загружает .fdt/.docx через браузер
2. Сервер распаковывает архив (adm-zip)
3. Извлекается `word/document.xml`
4. Regex парсинг всех полей: `/\{([^}]+)\}/g`
5. Возврат списка уникальных полей в UI
6. Пользователь выбирает префиксы из реальных данных
7. Генерируется config.json для CLI обработки

### CLI утилиты (трехэтапный pipeline)

**analyzer.js** → **generator.js** → **replacer.js**

Каждая утилита работает независимо, используя единый config.json  
Поддержка Base64-кодированных участков XML (метаданные Word)

---

## Технологии

- **Node.js** - runtime environment
- **Express 4.18.2** - веб-сервер
- **Multer 1.4.5** - загрузка файлов
- **adm-zip 0.5.10** - работа с архивами .docx/.fdt
- **Vanilla JS** - frontend без фреймворков

---

## Лицензия

Apache-2.0
